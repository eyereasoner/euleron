@prefix : <http://example.org/> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

# Reference: https://w3c.github.io/N3/spec/#grammar

# We can't have '<','>','{','}','|','^','`','\' in URLs.
# Also not when the are in the form of a UCHAR.
# But URI-encode is allowed
<http://bad\u0020example.org/> a :BadExample.

{
    :subject :predicate ?X.
}
=>
{
    :result :has :crash-syntax-8.
}.

{} => {
    :test :contains :crash-syntax-8.
}.

{
    :result :has :crash-syntax-8.
}
=>
{
    :test :is false.
}.